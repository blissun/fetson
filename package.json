{
  "name": "fetson",
  "version": "0.1.0",
  "description": "A lightweight HTTP client with complete type safety for TypeScript",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome lint ./src",
    "format": "biome format --write ./src",
    "check": "biome check --apply ./src",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "fetch",
    "http",
    "typescript",
    "api",
    "client",
    "request"
  ],
  "author": "blissun",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^20.17.30",
    "typescript": "~5.7.2",
    "vite": "^6.3.1",
    "vite-plugin-dts": "^3.9.1",
    "vitest": "^1.6.1"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
